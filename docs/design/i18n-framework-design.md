# 多语言框架重构设计文档

## 📋 概述

本文档描述了一个可扩展、自动化的多语言框架设计，旨在解决现有系统的痛点，并为未来扩展提供良好的基础。

## 🎯 设计目标

### 核心目标
1. **独立性** - 多语言系统作为独立模块，不依赖特定业务逻辑
2. **自动化** - 新增组件时自动生成多语言配置和类型定义
3. **扩展性** - 轻松添加新语言、新命名空间、新组件
4. **类型安全** - 完整的 TypeScript 类型支持，编译时检查翻译键
5. **开发体验** - 提供工具和 CLI，简化多语言开发流程

### 解决的问题
- ❌ 手动维护大量 import 语句
- ❌ 翻译键缺少类型检查，容易出错
- ❌ 新增组件需要手动创建多个语言文件
- ❌ 翻译键命名不规范，难以管理
- ❌ 缺少翻译完整性检查工具
- ❌ 无法自动检测未翻译的文本

## 🏗️ 架构设计

### 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                   应用层 (Components)                     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  Component A │  │  Component B │  │  Component C │  │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘  │
└─────────┼──────────────────┼──────────────────┼─────────┘
          │                  │                  │
          └──────────────────┼──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      i18n Framework Core             │
          │  ┌──────────────────────────────┐   │
          │  │   Translation Manager        │   │
          │  │   - 键值管理                  │   │
          │  │   - 命名空间管理              │   │
          │  │   - 语言切换                  │   │
          │  └──────────────────────────────┘   │
          │  ┌──────────────────────────────┐   │
          │  │   Type Generator              │   │
          │  │   - 自动生成类型定义           │   │
          │  │   - 类型安全检查              │   │
          │  └──────────────────────────────┘   │
          │  ┌──────────────────────────────┐   │
          │  │   Resource Loader             │   │
          │  │   - 动态加载语言资源           │   │
          │  │   - 懒加载支持                │   │
          │  └──────────────────────────────┘   │
          └──────────────────┬──────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      i18n Resources                 │
          │  ┌──────────┐  ┌──────────┐        │
          │  │ zh-CN/   │  │ en-US/   │        │
          │  │ ko-KR/   │  │ ja-JP/   │        │
          │  └──────────┘  └──────────┘        │
          └─────────────────────────────────────┘
                             │
          ┌──────────────────▼──────────────────┐
          │      Build Tools                    │
          │  - 扫描组件提取文本                  │
          │  - 生成翻译文件模板                 │
          │  - 验证翻译完整性                   │
          │  - 生成类型定义                     │
          └─────────────────────────────────────┘
```

### 目录结构

```
src/
├── i18n/                          # 多语言框架核心
│   ├── core/                      # 核心功能
│   │   ├── manager.ts            # 翻译管理器
│   │   ├── loader.ts             # 资源加载器
│   │   ├── generator.ts          # 类型生成器
│   │   └── validator.ts          # 验证器
│   ├── hooks/                     # React Hooks
│   │   ├── useTranslation.ts     # 增强的翻译Hook
│   │   ├── useLanguage.ts        # 语言切换Hook
│   │   └── useNamespace.ts       # 命名空间Hook
│   ├── utils/                     # 工具函数
│   │   ├── keyBuilder.ts         # 键构建器
│   │   ├── extractor.ts          # 文本提取器
│   │   └── formatter.ts          # 格式化工具
│   ├── types/                     # 类型定义
│   │   ├── keys.ts               # 自动生成的翻译键类型
│   │   ├── namespaces.ts         # 命名空间类型
│   │   └── languages.ts          # 语言类型
│   ├── config.ts                  # 框架配置
│   └── index.ts                   # 框架入口
│
├── i18n-resources/                 # 翻译资源（独立目录）
│   ├── components/                # 组件级翻译
│   │   ├── GameConfigPanel/
│   │   │   ├── zh-CN.json
│   │   │   ├── en-US.json
│   │   │   ├── ko-KR.json
│   │   │   └── ja-JP.json
│   │   ├── MultiPlayerGameBoard/
│   │   └── ...
│   ├── features/                  # 功能级翻译
│   │   ├── game/
│   │   ├── chat/
│   │   └── ...
│   ├── shared/                    # 共享翻译
│   │   ├── common/
│   │   ├── ui/
│   │   └── ...
│   └── metadata.json              # 元数据（语言列表、命名空间等）
│
└── scripts/                        # 构建工具
    ├── i18n/
    │   ├── scan.ts                # 扫描组件提取文本
    │   ├── generate.ts            # 生成翻译文件
    │   ├── validate.ts            # 验证翻译完整性
    │   ├── types.ts               # 生成类型定义
    │   └── cli.ts                 # CLI工具入口
    └── package.json               # 工具脚本配置
```

## 🔧 核心功能设计

### 1. 组件级翻译系统

#### 设计理念
每个组件拥有自己的翻译文件，实现模块化和可维护性。

#### 工作流程
```
1. 开发者在组件中使用翻译
   ↓
2. 构建工具扫描组件，提取翻译键
   ↓
3. 自动生成/更新翻译文件模板
   ↓
4. 生成类型定义，提供类型安全
   ↓
5. 运行时动态加载组件翻译
```

#### 示例结构

**组件文件：**
```typescript
// src/components/game/GameConfigPanel.tsx
import { useComponentTranslation } from '@/i18n/hooks';

export const GameConfigPanel: React.FC = () => {
  const { t } = useComponentTranslation('GameConfigPanel');
  
  return (
    <div>
      <h1>{t('title')}</h1>
      <button>{t('buttons.start')}</button>
    </div>
  );
};
```

**自动生成的翻译文件：**
```json
// i18n-resources/components/GameConfigPanel/zh-CN.json
{
  "title": "游戏配置",
  "buttons": {
    "start": "开始游戏",
    "cancel": "取消"
  }
}
```

**自动生成的类型：**
```typescript
// src/i18n/types/keys.ts (自动生成)
export interface GameConfigPanelKeys {
  title: string;
  buttons: {
    start: string;
    cancel: string;
  };
}
```

### 2. 命名空间系统

#### 层级结构
```
命名空间层级：
├── component:GameConfigPanel      # 组件级
├── feature:game                   # 功能级
└── shared:common                  # 共享级
```

#### 命名规范
- **组件命名空间**: `component:{ComponentName}`
- **功能命名空间**: `feature:{FeatureName}`
- **共享命名空间**: `shared:{CategoryName}`

#### 使用方式
```typescript
// 组件级翻译（自动命名空间）
const { t } = useComponentTranslation('GameConfigPanel');
t('title'); // 自动解析为 component:GameConfigPanel:title

// 功能级翻译
const { t } = useFeatureTranslation('game');
t('actions.start'); // 解析为 feature:game:actions.start

// 共享翻译
const { t } = useSharedTranslation('common');
t('buttons.confirm'); // 解析为 shared:common:buttons.confirm
```

### 3. 类型安全系统

#### 类型生成策略
1. **扫描阶段**: 扫描所有翻译文件
2. **分析阶段**: 分析翻译键结构
3. **生成阶段**: 生成 TypeScript 类型定义
4. **验证阶段**: 编译时检查翻译键使用

#### 类型定义示例
```typescript
// 自动生成的类型
export type TranslationKey = 
  | `component:GameConfigPanel:${keyof GameConfigPanelKeys}`
  | `feature:game:${keyof GameKeys}`
  | `shared:common:${keyof CommonKeys}`;

// 类型安全的翻译函数
function t(key: TranslationKey, options?: InterpolationOptions): string;
```

### 4. 自动化工具链

#### 工具列表

**1. 扫描工具 (`scan.ts`)**
- 扫描组件文件，提取硬编码文本
- 识别需要翻译的字符串
- 生成翻译键建议

**2. 生成工具 (`generate.ts`)**
- 根据扫描结果生成翻译文件模板
- 为新组件创建所有语言的翻译文件
- 更新现有翻译文件结构

**3. 验证工具 (`validate.ts`)**
- 检查翻译完整性（所有语言是否都有对应键）
- 检查未使用的翻译键
- 检查缺失的翻译

**4. 类型生成工具 (`types.ts`)**
- 从翻译文件生成 TypeScript 类型
- 更新类型定义文件
- 验证类型一致性

**5. CLI 工具 (`cli.ts`)**
```bash
# 扫描新组件
npm run i18n:scan --component GameConfigPanel

# 生成翻译文件
npm run i18n:generate --component GameConfigPanel

# 验证翻译完整性
npm run i18n:validate

# 生成类型定义
npm run i18n:types

# 完整流程（扫描+生成+验证+类型）
npm run i18n:sync
```

## 📐 详细设计

### 1. 翻译管理器 (Translation Manager)

#### 职责
- 管理所有翻译资源
- 处理命名空间解析
- 提供翻译查找接口
- 支持动态语言切换

#### API 设计
```typescript
class TranslationManager {
  // 初始化
  init(config: I18nConfig): Promise<void>;
  
  // 加载资源
  loadNamespace(namespace: string, language: string): Promise<void>;
  
  // 获取翻译
  translate(key: string, options?: InterpolationOptions): string;
  
  // 切换语言
  changeLanguage(language: string): Promise<void>;
  
  // 获取当前语言
  getCurrentLanguage(): string;
  
  // 检查翻译是否存在
  hasTranslation(key: string): boolean;
}
```

### 2. 资源加载器 (Resource Loader)

#### 设计特点
- **懒加载**: 按需加载组件翻译
- **缓存机制**: 已加载的资源缓存
- **预加载**: 支持预加载常用翻译
- **错误处理**: 优雅降级到默认语言

#### 加载策略
```typescript
// 加载优先级
1. 组件级翻译（component:ComponentName）
2. 功能级翻译（feature:FeatureName）
3. 共享翻译（shared:CategoryName）
4. 回退到默认语言
```

### 3. 类型生成器 (Type Generator)

#### 生成流程
```
翻译文件 (JSON)
    ↓
解析结构
    ↓
生成类型定义 (TypeScript)
    ↓
验证类型一致性
    ↓
输出类型文件
```

#### 生成规则
- 扁平键转换为嵌套对象类型
- 支持插值参数类型推断
- 生成完整的类型联合

### 4. 验证器 (Validator)

#### 验证项
1. **完整性检查**
   - 所有语言是否都有相同的键
   - 是否有缺失的翻译

2. **一致性检查**
   - 键名是否规范
   - 命名空间是否正确

3. **使用检查**
   - 是否有未使用的翻译键
   - 是否有未翻译的硬编码文本

## 🔄 迁移策略

### 阶段一：框架搭建（1-2天）
1. 创建新的目录结构
2. 实现核心管理器
3. 实现资源加载器
4. 创建基础工具

### 阶段二：工具开发（2-3天）
1. 开发扫描工具
2. 开发生成工具
3. 开发验证工具
4. 开发类型生成工具
5. 开发 CLI 工具

### 阶段三：迁移现有翻译（2-3天）
1. 迁移现有翻译文件到新结构
2. 更新组件使用新 API
3. 生成类型定义
4. 验证迁移结果

### 阶段四：自动化集成（1-2天）
1. 集成到构建流程
2. 添加开发时自动检查
3. 配置 CI/CD 验证

### 阶段五：文档和优化（1天）
1. 编写使用文档
2. 优化性能
3. 添加示例代码

## 🎨 使用示例

### 场景1: 新增组件

**步骤1**: 创建组件
```typescript
// src/components/new/NewComponent.tsx
import { useComponentTranslation } from '@/i18n/hooks';

export const NewComponent: React.FC = () => {
  const { t } = useComponentTranslation('NewComponent');
  
  return (
    <div>
      <h1>{t('title')}</h1>
      <p>{t('description')}</p>
    </div>
  );
};
```

**步骤2**: 运行扫描工具
```bash
npm run i18n:scan --component NewComponent
```

**步骤3**: 自动生成翻译文件
```bash
npm run i18n:generate --component NewComponent
```

**步骤4**: 填写翻译内容
```json
// i18n-resources/components/NewComponent/zh-CN.json
{
  "title": "新组件",
  "description": "这是一个新组件"
}
```

**步骤5**: 类型自动更新
```bash
npm run i18n:types
```

### 场景2: 添加新语言

**步骤1**: 更新配置
```typescript
// src/i18n/config.ts
export const supportedLanguages = [
  // ... 现有语言
  { code: 'fr-FR', name: 'Français', flag: '🇫🇷' },
];
```

**步骤2**: 运行生成工具
```bash
npm run i18n:generate --language fr-FR
```

**步骤3**: 自动创建所有翻译文件模板
```
i18n-resources/
├── components/
│   └── GameConfigPanel/
│       └── fr-FR.json  # 自动创建
└── ...
```

### 场景3: 验证翻译完整性

```bash
# 检查所有翻译
npm run i18n:validate

# 输出报告
✓ 所有语言翻译完整
✗ GameConfigPanel:title 在 fr-FR 中缺失
✗ 发现 3 个未使用的翻译键
```

## 📊 优势对比

### 现有系统 vs 新框架

| 特性 | 现有系统 | 新框架 |
|------|---------|--------|
| 手动维护 | ❌ 需要手动导入 | ✅ 自动生成 |
| 类型安全 | ❌ 无类型检查 | ✅ 完整类型支持 |
| 组件隔离 | ❌ 全局命名空间 | ✅ 组件级命名空间 |
| 扩展性 | ⚠️ 需要手动添加 | ✅ 自动化工具 |
| 验证 | ❌ 无验证工具 | ✅ 完整验证工具链 |
| 开发体验 | ⚠️ 一般 | ✅ 优秀 |

## 🚀 未来扩展

### 计划功能
1. **翻译管理平台**: Web界面管理翻译
2. **自动翻译**: 集成翻译API自动翻译
3. **翻译版本控制**: 管理翻译历史
4. **翻译质量检查**: AI检查翻译质量
5. **多语言测试**: 自动化多语言测试

## 📝 实施步骤

### 第一步：设计确认
- [ ] 评审设计文档
- [ ] 确认技术方案
- [ ] 确定实施优先级

### 第二步：核心开发
- [ ] 实现 TranslationManager
- [ ] 实现 ResourceLoader
- [ ] 实现 TypeGenerator
- [ ] 实现 Validator

### 第三步：工具开发
- [ ] 开发扫描工具
- [ ] 开发生成工具
- [ ] 开发验证工具
- [ ] 开发 CLI 工具

### 第四步：迁移
- [ ] 迁移现有翻译
- [ ] 更新组件使用
- [ ] 生成类型定义

### 第五步：测试和优化
- [ ] 功能测试
- [ ] 性能优化
- [ ] 文档完善

## ❓ 待讨论问题

1. **命名空间策略**: 是否采用组件级命名空间？
2. **类型生成时机**: 编译时还是运行时？
3. **翻译文件格式**: JSON 还是其他格式？
4. **懒加载策略**: 何时加载组件翻译？
5. **向后兼容**: 如何兼容现有代码？

## 📚 参考资料

- react-i18next 文档
- i18next 最佳实践
- TypeScript 类型生成技术
- 其他项目的多语言方案

