# 选中牌光晕效果优化

## 修改时间
2024年12月2日

## 问题
选中牌的高亮光晕效果过大，视觉效果过于夸张。

## 解决方案
减小所有选中牌和可出牌的光晕效果，使其更加精致和舒适。

---

## 修改文件

### 1. CardComponent.css
**位置**：`.card-selected` 样式

**修改前**：
```css
box-shadow: 0 4px 12px rgba(76, 175, 80, 0.5);
```

**修改后**：
```css
box-shadow: 0 2px 6px rgba(76, 175, 80, 0.4);
```

**效果**：
- 阴影大小：12px → 6px（减少50%）
- 透明度：0.5 → 0.4（降低20%）

---

### 2. SimplifiedHandCards.css

#### 2.1 可出牌高亮（playable）

**位置**：`.rank-item.playable` 样式

**修改前**：
```css
border: 3px solid rgba(255, 193, 7, 1);
box-shadow: 
  0 0 20px rgba(255, 193, 7, 0.8),
  0 0 40px rgba(255, 193, 7, 0.5),
  inset 0 0 30px rgba(255, 193, 7, 0.3),
  0 4px 15px rgba(255, 193, 7, 0.6);
background: rgba(255, 193, 7, 0.15);
filter: drop-shadow(0 0 15px rgba(255, 193, 7, 1));
transform: scale(1.05);
```

**修改后**：
```css
border: 2px solid rgba(255, 193, 7, 0.9);
box-shadow: 
  0 0 8px rgba(255, 193, 7, 0.5),
  0 2px 8px rgba(255, 193, 7, 0.3);
background: rgba(255, 193, 7, 0.08);
filter: drop-shadow(0 0 6px rgba(255, 193, 7, 0.6));
transform: scale(1.03);
```

**效果**：
- 边框：3px → 2px（减少33%）
- 外光晕：20px/40px → 8px（减少60-80%）
- 背景光晕：0.15 → 0.08（减少47%）
- 整体发光：15px → 6px（减少60%）
- 缩放：1.05 → 1.03（减少2%）

#### 2.2 发光环效果

**位置**：`.rank-item.playable::before` 样式

**修改前**：
```css
top: -8px; left: -8px; right: -8px; bottom: -8px;
border: 2px solid rgba(255, 193, 7, 0.6);
```

**修改后**：
```css
top: -4px; left: -4px; right: -4px; bottom: -4px;
border: 1px solid rgba(255, 193, 7, 0.4);
```

**效果**：
- 环大小：8px → 4px（减少50%）
- 边框粗细：2px → 1px（减少50%）
- 透明度：0.6 → 0.4（降低33%）

#### 2.3 脉冲动画

**位置**：`@keyframes playable-pulse` 动画

**修改前**：
```css
0%, 100% {
  filter: drop-shadow(0 0 15px rgba(255, 193, 7, 1));
  box-shadow: 0 0 20px..., 0 0 40px..., ...;
}
50% {
  filter: drop-shadow(0 0 25px rgba(255, 193, 7, 1.2));
  box-shadow: 0 0 30px..., 0 0 60px..., ...;
}
```

**修改后**：
```css
0%, 100% {
  filter: drop-shadow(0 0 6px rgba(255, 193, 7, 0.6));
  box-shadow: 0 0 8px..., 0 2px 8px...;
}
50% {
  filter: drop-shadow(0 0 10px rgba(255, 193, 7, 0.8));
  box-shadow: 0 0 12px..., 0 3px 10px...;
}
```

**效果**：
- 初始光晕：15px → 6px（减少60%）
- 峰值光晕：25px → 10px（减少60%）
- 动画幅度更小，更精致

#### 2.4 选中卡牌

**位置**：`.rank-card-stacked.card-selected` 样式

**修改前**：
```css
filter: drop-shadow(0 2px 6px rgba(76, 175, 80, 0.6));

@keyframes card-selected-pulse {
  0%, 100% { filter: drop-shadow(0 2px 6px rgba(76, 175, 80, 0.6)); }
  50% { filter: drop-shadow(0 4px 12px rgba(76, 175, 80, 0.9)); }
}
```

**修改后**：
```css
filter: drop-shadow(0 1px 4px rgba(76, 175, 80, 0.5));

@keyframes card-selected-pulse {
  0%, 100% { filter: drop-shadow(0 1px 4px rgba(76, 175, 80, 0.5)); }
  50% { filter: drop-shadow(0 2px 6px rgba(76, 175, 80, 0.7)); }
}
```

**效果**：
- 初始阴影：6px → 4px（减少33%）
- 峰值阴影：12px → 6px（减少50%）
- 透明度略微降低

---

### 3. CompactHandCards.css

#### 3.1 叠放模式选中

**位置**：`.compact-card-item-stacked.selected` 样式

**修改前**：
```css
filter: drop-shadow(0 0 20px rgba(76, 175, 80, 0.8));
```

**修改后**：
```css
filter: drop-shadow(0 0 8px rgba(76, 175, 80, 0.6));
```

**效果**：
- 光晕大小：20px → 8px（减少60%）
- 透明度：0.8 → 0.6（降低25%）

#### 3.2 展开模式选中

**位置**：`.expanded-card-item.selected` 样式

**修改前**：
```css
filter: drop-shadow(0 0 20px rgba(76, 175, 80, 0.8));
```

**修改后**：
```css
filter: drop-shadow(0 0 8px rgba(76, 175, 80, 0.6));
```

**效果**：
- 光晕大小：20px → 8px（减少60%）
- 透明度：0.8 → 0.6（降低25%）

---

## 优化总结

### 光晕尺寸变化

| 效果类型 | 修改前 | 修改后 | 减少幅度 |
|---------|--------|--------|---------|
| **基础选中** | 12px | 6px | -50% |
| **可出牌高亮** | 20-40px | 8px | -60~80% |
| **脉冲峰值** | 25-60px | 10-12px | -50~80% |
| **发光环** | 8px边距 | 4px边距 | -50% |
| **叠放选中** | 20px | 8px | -60% |

### 整体效果

- ✅ **视觉更精致** - 光晕不再过度夸张
- ✅ **层次更清晰** - 减少视觉干扰
- ✅ **性能更好** - 减少渲染负担
- ✅ **保留功能** - 仍然清晰可见

---

## 视觉对比

### 修改前
- 🔆 光晕范围：20-60px
- 🔆 多层叠加阴影
- 🔆 强烈的发光效果
- 🔆 大范围的背景光晕

### 修改后
- ✨ 光晕范围：4-10px
- ✨ 精简的阴影效果
- ✨ 柔和的发光效果
- ✨ 适度的背景提示

---

## 使用效果

刷新浏览器后，选中牌的效果将变得：
- ✅ 更精致优雅
- ✅ 更符合现代UI设计
- ✅ 减少视觉疲劳
- ✅ 保持功能可见性

**建议**：刷新页面查看新效果，如需进一步微调可以继续调整。

