# 🌐 外网访问完整配置指南

> **配置日期**: 2025年12月4日  
> **公网IP**: 115.93.10.51  
> **内网服务器**: 192.168.0.13 (server4)  
> **状态**: ✅ 已完成并测试通过

## 📋 配置概览

本文档记录了如何配置 MeLo TTS 和 Ollama 服务的外网访问。

### 服务信息

| 服务 | 端口 | 内网地址 | 外网地址 |
|------|------|---------|---------|
| **MeLo TTS** | 7860 | http://192.168.0.13:7860 | http://115.93.10.51:7860 |
| **Ollama** | 11434 | http://192.168.0.13:11434 | http://115.93.10.51:11434 |

---

## 🚀 配置步骤

### 第一步：查看公网IP

```bash
# SSH 登录到服务器
ssh hlsystem@192.168.0.13

# 查看公网IP
curl ifconfig.me
# 输出: 115.93.10.51
```

**验证是否为真实公网IP**：
- ✅ 如果是类似 `115.93.10.51` 这样的地址，是公网IP
- ❌ 如果是 `10.x.x.x`、`172.16-31.x.x`、`192.168.x.x`、`100.x.x.x`，是内网IP

### 第二步：检查服务状态

```bash
# 查看服务器内网IP
ip addr show | grep "inet " | grep -v "127.0.0.1"
# 输出包含: inet 192.168.0.13/24

# 检查 Ollama 位置和状态
which ollama
# 输出: /usr/local/bin/ollama

ps aux | grep ollama
# 应该看到 ollama serve 进程在运行

# 检查 MeLo TTS 状态
ps aux | grep melo
# 应该看到 python3 melo-multilang.py 进程在运行

# 检查 MeLo TTS 安装位置
ls -la ~/melotts/
# 应该看到 MeloTTS 目录
```

### 第三步：配置 Ollama 监听地址

确保 Ollama 监听所有网络接口 (0.0.0.0)，而不仅仅是 localhost：

```bash
# 检查当前监听地址
sudo ss -tlnp | grep 11434
# 应该显示: *:11434 或 0.0.0.0:11434

# 如果显示 127.0.0.1:11434，需要重新配置
sudo systemctl stop ollama

# 创建/更新 systemd 服务配置
sudo tee /etc/systemd/system/ollama.service > /dev/null << 'EOF'
[Unit]
Description=Ollama LLM Service
After=network.target

[Service]
Type=simple
User=ollama
Environment="OLLAMA_HOST=0.0.0.0:11434"
ExecStart=/usr/local/bin/ollama serve
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
EOF

# 重新加载并启动
sudo systemctl daemon-reload
sudo systemctl enable ollama
sudo systemctl start ollama

# 验证监听地址
sudo ss -tlnp | grep 11434
# 现在应该显示: *:11434
```

### 第四步：检查 MeLo TTS 监听地址

```bash
# 检查 MeLo TTS 监听地址
sudo ss -tlnp | grep 7860
# 应该显示: 0.0.0.0:7860

# 如果 MeLo TTS 未运行，启动它
screen -ls  # 查看是否有 melo 会话
screen -r melo  # 连接到会话查看
# 按 Ctrl+A 然后 D 退出（不停止程序）
```

### 第五步：配置防火墙

```bash
# 开放端口
sudo ufw allow 7860/tcp comment "MeLo TTS"
sudo ufw allow 11434/tcp comment "Ollama"

# 查看防火墙状态
sudo ufw status

# 如果防火墙是 inactive，无需额外操作
# 如果是 active，确保端口已开放
```

### 第六步：测试本地访问

```bash
# 测试 MeLo TTS
curl http://localhost:7860/health
# 期望输出: {"status":"ok","service":"Melo TTS Multi",...}

# 测试 Ollama
curl http://localhost:11434/api/tags
# 期望输出: {"models":[...]}

# 测试内网访问
curl http://192.168.0.13:7860/health
curl http://192.168.0.13:11434/api/tags
```

如果本地测试都通过，服务端配置就完成了！✅

### 第七步：配置路由器端口转发

#### 1. 登录路由器

浏览器打开路由器管理界面（常见地址）：
- http://192.168.0.1
- http://192.168.1.1
- http://192.168.31.1 (小米路由器)

#### 2. 切换语言（如果需要）

如果路由器是韩文或其他语言，查找：
- **语言设置**: 시스템 관리 → 언어 / System → Language
- 通常在右上角有语言选择图标 🌐

切换为**中文**或**English**

#### 3. 找到端口转发设置

**中文界面查找**：
- "高级设置" → "端口转发"
- "高级设置" → "虚拟服务器"
- "NAT设置" → "端口映射"

**英文界面查找**：
- "Advanced" → "Port Forwarding"
- "Advanced" → "Virtual Server"
- "Security" → "Port Forwarding"

#### 4. 添加端口转发规则

点击 "Add" 或 "新增" 或 "+ Add new rule"，添加以下两条规则：

**规则1：MeLo TTS**
```
服务名称/Rule Name: MeLo-TTS
外部端口/External Port: 7860 (或 7860~7860)
内部IP/Internal IP: 192.168.0.13
内部端口/Internal Port: 7860 (或 7860~7860)
协议/Protocol: TCP
启用/Enable: ✓ (勾选)
```

**规则2：Ollama**
```
服务名称/Rule Name: Ollama
外部端口/External Port: 11434 (或 11434~11434)
内部IP/Internal IP: 192.168.0.13
内部端口/Internal Port: 11434 (或 11434~11434)
协议/Protocol: TCP
启用/Enable: ✓ (勾选)
```

#### 5. 保存配置

点击 "Apply" 或 "保存" 按钮应用配置。

### 第八步：测试外网访问

配置完成后，从任何地方（手机、电脑等，不在内网也可以）测试：

```bash
# 测试 MeLo TTS
curl http://115.93.10.51:7860/health

# 测试 Ollama
curl http://115.93.10.51:11434/api/tags

# 测试 TTS 功能
curl -X POST http://115.93.10.51:7860/tts \
  -H "Content-Type: application/json" \
  -d '{"text": "外网访问测试成功", "lang": "ZH"}' \
  --output test-external.wav
```

**或者用浏览器访问**：
- http://115.93.10.51:7860/health
- http://115.93.10.51:7860/docs (MeLo TTS API文档)
- http://115.93.10.51:11434/api/tags

如果能访问，配置就成功了！🎉

### 第九步：修改应用配置

修改游戏应用配置，使用公网IP：

#### 修改 TTS 配置

**文件**: `src/App.tsx`

```typescript
// 第 98 行
config.meloConfig = {
  baseUrl: 'http://115.93.10.51:7860',  // 改为公网IP
  timeout: 30000,
  retryCount: 2,
  defaultSpeaker: 'ZH',
};
```

#### 修改 Ollama 配置

**文件**: `src/config/chatConfig.ts`

```typescript
// 第 89 行
export const DEFAULT_LLM_CHAT_CONFIG: LLMChatConfig = {
  provider: 'custom',
  apiUrl: 'http://115.93.10.51:11434/api/chat',  // 改为公网IP
  model: 'qwen2.5:3b',
  // ... 其他配置
};
```

### 第十步：修复代码问题（如遇到）

如果启动应用时遇到 `serverManager.checkServer is not a function` 错误：

**问题**: `OllamaServerManager` 类缺少 `checkServer` 方法

**解决方法**: 在 `src/services/llm/OllamaServerManager.ts` 中添加以下方法：

```typescript
/**
 * 检查服务器可用性
 */
async checkServer(server: OllamaServerConfig): Promise<ServerCheckResult> {
  const startTime = Date.now();
  
  try {
    const url = this.getServerTagsUrl(server);
    const response = await fetch(url, {
      method: 'GET',
      headers: { 'Content-Type': 'application/json' },
      signal: AbortSignal.timeout(5000) // 5秒超时
    });

    if (!response.ok) {
      return {
        available: false,
        error: `HTTP ${response.status}: ${response.statusText}`
      };
    }

    const data = await response.json();
    const latency = Date.now() - startTime;
    
    const result: ServerCheckResult = {
      available: true,
      latency,
      modelCount: data.models?.length || 0
    };
    
    // 更新服务器状态
    this.updateServerStatus(server.id, result);
    
    return result;
  } catch (error) {
    const result: ServerCheckResult = {
      available: false,
      error: error instanceof Error ? error.message : '连接失败'
    };
    
    // 更新服务器状态
    this.updateServerStatus(server.id, result);
    
    return result;
  }
}
```

### 第十一步：重启应用

```bash
# 在项目目录
cd ~/guozha_poker_game

# 停止应用（如果在运行）
# 按 Ctrl+C

# 重新启动
npm run dev
```

或者直接在浏览器刷新页面。

---

## ✅ 验证清单

配置完成后，确保以下所有项都通过：

- [x] **公网IP确认**: 115.93.10.51
- [x] **Ollama 监听**: `sudo ss -tlnp | grep 11434` 显示 `*:11434`
- [x] **MeLo TTS 监听**: `sudo ss -tlnp | grep 7860` 显示 `0.0.0.0:7860`
- [x] **防火墙配置**: 端口 7860 和 11434 已开放
- [x] **本地测试**: `curl http://localhost:7860/health` 通过
- [x] **本地测试**: `curl http://localhost:11434/api/tags` 通过
- [x] **路由器配置**: 端口转发规则已添加
- [x] **外网测试**: `curl http://115.93.10.51:7860/health` 通过
- [x] **外网测试**: `curl http://115.93.10.51:11434/api/tags` 通过
- [x] **应用配置**: `src/App.tsx` 已更新
- [x] **应用配置**: `src/config/chatConfig.ts` 已更新
- [x] **代码修复**: `checkServer` 方法已添加
- [x] **应用启动**: 无错误，正常运行

---

## 🔍 故障排查

### 问题1：外网无法访问

**症状**: 本地可以访问，但外网访问超时

**排查步骤**:
1. 确认路由器端口转发配置正确
2. 确认公网IP没有变化（动态IP可能会变）
3. 检查是否有多层路由器（光猫需要桥接模式）
4. 尝试重启路由器

### 问题2：Ollama 只监听 localhost

**症状**: `ss -tlnp | grep 11434` 显示 `127.0.0.1:11434`

**解决**:
```bash
sudo systemctl stop ollama
# 重新配置 systemd 服务（见第三步）
sudo systemctl start ollama
```

### 问题3：防火墙阻止连接

**症状**: 本地可以访问，内网其他设备无法访问

**解决**:
```bash
sudo ufw allow 7860/tcp
sudo ufw allow 11434/tcp
sudo ufw reload
```

### 问题4：端口被占用

**症状**: 服务启动失败，提示端口已被占用

**解决**:
```bash
# 查看占用端口的进程
sudo lsof -i :7860
sudo lsof -i :11434

# 停止相关进程或更换端口
```

### 问题5：公网IP变化

**症状**: 之前可以访问，现在访问不了

**解决**:
```bash
# 重新查看公网IP
curl ifconfig.me

# 如果IP变了，更新应用配置
```

---

## 🔒 安全建议

### 1. 使用 API Key 认证（推荐）

为 MeLo TTS 添加 API Key 认证，防止滥用。

### 2. 限制访问IP

如果只有固定IP需要访问，在防火墙中限制：

```bash
sudo ufw delete allow 7860/tcp
sudo ufw delete allow 11434/tcp
sudo ufw allow from 你的办公室IP to any port 7860 proto tcp
sudo ufw allow from 你的办公室IP to any port 11434 proto tcp
```

### 3. 使用 HTTPS（推荐生产环境）

使用 Nginx 反向代理 + Let's Encrypt 证书。

### 4. 定期检查日志

```bash
# Ollama 日志
sudo journalctl -u ollama -f

# MeLo TTS 日志
tail -f ~/melotts.log

# 系统日志
sudo tail -f /var/log/syslog
```

### 5. 使用 VPN（最安全）

如果是团队内部使用，建议搭建 VPN，避免直接暴露服务。

---

## 📊 服务管理

### 启动服务

```bash
# Ollama
sudo systemctl start ollama

# MeLo TTS（如果未在 screen 中运行）
cd ~/melotts/MeloTTS
screen -S melo
python3 melo-multilang.py
# 按 Ctrl+A, D 退出 screen
```

### 停止服务

```bash
# Ollama
sudo systemctl stop ollama

# MeLo TTS
screen -r melo
# 在 screen 中按 Ctrl+C 停止
# 或直接杀进程
pkill -f melo-multilang
```

### 重启服务

```bash
# Ollama
sudo systemctl restart ollama

# MeLo TTS
screen -r melo
# 按 Ctrl+C 停止，然后重新运行
python3 melo-multilang.py
```

### 查看服务状态

```bash
# Ollama 状态
sudo systemctl status ollama

# 查看所有相关进程
ps aux | grep -E "ollama|melo"

# 查看端口监听
sudo ss -tlnp | grep -E "7860|11434"
```

### 查看日志

```bash
# Ollama 日志
sudo journalctl -u ollama -n 100 --no-pager

# 实时查看 Ollama 日志
sudo journalctl -u ollama -f

# MeLo TTS 日志（如果重定向到文件）
tail -f ~/melotts.log
```

### 设置开机自启

```bash
# Ollama 开机自启（已配置）
sudo systemctl enable ollama

# MeLo TTS 开机自启（使用 crontab）
crontab -e
# 添加：
# @reboot cd ~/melotts/MeloTTS && screen -dmS melo python3 melo-multilang.py
```

---

## 📝 配置文件位置

| 项目 | 位置 |
|------|------|
| **Ollama 服务配置** | `/etc/systemd/system/ollama.service` |
| **MeLo TTS 安装目录** | `~/melotts/MeloTTS/` |
| **游戏项目 TTS 配置** | `src/App.tsx` (第98行) |
| **游戏项目 Ollama 配置** | `src/config/chatConfig.ts` (第89行) |
| **服务器管理器代码** | `src/services/llm/OllamaServerManager.ts` |

---

## 🌐 访问地址总结

### 内网访问
- **MeLo TTS**: http://192.168.0.13:7860
- **Ollama**: http://192.168.0.13:11434

### 外网访问
- **MeLo TTS**: http://115.93.10.51:7860
- **MeLo TTS API 文档**: http://115.93.10.51:7860/docs
- **Ollama**: http://115.93.10.51:11434

### API 端点
- **MeLo TTS Health**: `GET http://115.93.10.51:7860/health`
- **MeLo TTS Synthesize**: `POST http://115.93.10.51:7860/tts`
- **Ollama Models**: `GET http://115.93.10.51:11434/api/tags`
- **Ollama Chat**: `POST http://115.93.10.51:11434/api/chat`

---

## 📅 配置历史

| 日期 | 操作 | 状态 |
|------|------|------|
| 2025-12-04 | 查看公网IP | ✅ 115.93.10.51 |
| 2025-12-04 | 配置 Ollama 监听地址 | ✅ 完成 |
| 2025-12-04 | 配置防火墙 | ✅ 完成 |
| 2025-12-04 | 配置路由器端口转发 | ✅ 完成 |
| 2025-12-04 | 测试外网访问 | ✅ 通过 |
| 2025-12-04 | 修改应用配置 | ✅ 完成 |
| 2025-12-04 | 修复 checkServer 方法 | ✅ 完成 |
| 2025-12-04 | 应用测试 | ✅ 成功运行 |

---

## 💡 最佳实践

1. **定期检查公网IP**: 如果是动态IP，可能会变化
2. **监控服务状态**: 设置监控告警
3. **备份配置**: 定期备份路由器和服务器配置
4. **日志管理**: 定期清理和归档日志
5. **安全更新**: 及时更新 Ollama 和系统安全补丁
6. **性能监控**: 监控服务器资源使用情况
7. **访问控制**: 根据需要限制访问IP和频率

---

## 📞 支持

如有问题，可以：
1. 查看本文档的故障排查部分
2. 检查服务日志
3. 参考相关文档：
   - `docs/setup/melo-tts-remote-connection.md`
   - `在0.13上启动TTS.md`
   - `外网访问配置指南.md`

---

**配置完成！** 🎉

现在你可以从任何地方访问 MeLo TTS 和 Ollama 服务了！

