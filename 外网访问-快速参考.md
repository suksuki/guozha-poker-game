# 🚀 外网访问 - 快速参考

> **公网IP**: 115.93.10.51  
> **服务器**: 192.168.0.13 (server4)  
> **状态**: ✅ 已配置完成

## 📍 访问地址

| 服务 | 外网地址 | 内网地址 |
|------|---------|---------|
| **MeLo TTS** | http://115.93.10.51:7860 | http://192.168.0.13:7860 |
| **Ollama** | http://115.93.10.51:11434 | http://192.168.0.13:11434 |

## 🔧 快速命令

### 查看公网IP
```bash
curl ifconfig.me
```

### 检查服务状态
```bash
# 检查监听端口
sudo ss -tlnp | grep -E "7860|11434"

# 检查进程
ps aux | grep -E "ollama|melo"

# 检查 Ollama 服务
sudo systemctl status ollama

# 测试本地访问
curl http://localhost:7860/health
curl http://localhost:11434/api/tags

# 测试外网访问
curl http://115.93.10.51:7860/health
curl http://115.93.10.51:11434/api/tags
```

### 重启服务
```bash
# 重启 Ollama
sudo systemctl restart ollama

# 重启 MeLo TTS（在 screen 会话中）
screen -r melo
# 按 Ctrl+C 停止，然后重新运行
python3 melo-multilang.py
# 按 Ctrl+A, D 退出
```

## 📝 配置文件

- **应用 TTS 配置**: `src/App.tsx` (第98行) → `http://115.93.10.51:7860`
- **应用 Ollama 配置**: `src/config/chatConfig.ts` (第89行) → `http://115.93.10.51:11434`
- **Ollama 服务**: `/etc/systemd/system/ollama.service`

## 🔍 故障排查

### 外网无法访问？
1. 检查路由器端口转发配置
2. 确认公网IP没有变化
3. 检查防火墙规则

### 服务无法启动？
```bash
# 查看日志
sudo journalctl -u ollama -n 50
tail -f ~/melotts.log
```

## 📚 完整文档

详细配置过程请查看：`docs/setup/外网访问完整配置指南.md`

---

**配置完成日期**: 2025年12月4日

